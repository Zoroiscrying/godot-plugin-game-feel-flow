# Free showcase combos

`GFFCombo` resources used by `examples/showcase.tscn` → `Subject/GFFPlayer.combo_dictionary`.

Regenerate after editing the baker:

```bash
godot --headless --path . --script res://tools/bake_showcase_combos.gd
```
